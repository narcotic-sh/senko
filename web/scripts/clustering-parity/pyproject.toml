[project]
name = "senko-clustering-parity-tools"
version = "0.1.0"
description = "Native clustering fixture generation for Senko browser parity work"
requires-python = ">=3.13,<3.14"
dependencies = [
    "hdbscan==0.8.44",
    "numpy==2.4.6",
    "pyyaml==6.0.3",
    "scikit-learn==1.9.0",
    "umap-learn==0.5.12",
]

[tool.uv]
package = false
